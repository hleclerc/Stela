def add a, b
    ___add a, b

def not_boolean v
    ___not_boolean v
    
def inferior a, b
    ___info a, b
    ___inf a, b
    
class SI32
    ___set_base_size 32
    ___set_base_alig 32
    def init val
        ___reassign_rec self, val
    def reassign val
        ___reassign_rec self, val

void := 1

def init self
    # nothing to do
    
def typeof val
    ___typeof val

class Range[ TB, TE, TI ]
    beg ~= TB
    end ~= TE
    inc ~= TI
    def init( b, e, i ) starts_with beg( b ), end( e ), inc( i )
    
def range a, b
    ___disp Range[ SI32, SI32, SI32 ]
    # ___disp Range[ typeof( a ), typeof( b ), typeof( a ) ]
    # Range[ typeof( a ), typeof( b ), typeof( a ) ]( a, b, 1 )

class A[ T, U ]
    a := 10
    
i := A[ 20, 39 ]()
___disp i.a
___disp i.T
# r := 0 .. 10
# ___info r
# for a in 0 .. 10
    # ___syscall a
